"use client";

import { useEffect } from "react";
// import { useRouter } from "next/navigation";

export const GlobalRedirect = ({ to = "1", ...props }: { to: string }) => {
    // const router = useRouter();

    // useEffect(() => {
    //     setTimeout(() => {
    //         router.push(to);
    //     }, 0);
    // }, [to, router]);

    return null;
};
